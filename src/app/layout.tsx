import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Qui veut gagner 20 millions ? — Quiz multijoueur",
  description: "Plateforme de quiz multijoueur en temps réel. Affrontez d'autres joueurs, montez dans le classement et enrichissez vos connaissances.",
  keywords: ["quiz", "multijoueur", "culture générale", "jeu en ligne", "temps réel"],
  authors: [{ name: "QVGDM" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="fr" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
